services:
  site:
    environment:
      ENVIRONMENT: testing
    volumes:
      - site_config:/var/www/foxcaves/config
      - site_storage:/var/www/foxcaves/storage
  redis:
    volumes:
      - redis:/data
  mysql:
    volumes:
      - mysql:/var/lib/mysql

volumes:
  site_config: {}
  site_storage: {}
  redis: {}
  mysql: {}
